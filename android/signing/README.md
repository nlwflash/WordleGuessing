Local Android signing material lives in this directory.

- Keep the real keystore file here, but do not commit it.
- The repo ignores `*.jks`, `*.keystore`, and `*.pem` in this directory.
- Back the keystore up outside the repository before sharing signed APKs.
